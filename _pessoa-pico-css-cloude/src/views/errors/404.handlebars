<div class="container error-page">
    <article class="text-center">
        <header>
            <h1>404</h1>
            <p class="error-subtitle">Página não encontrada</p>
        </header>

        <div class="error-content">
            <p>A página que você está procurando não existe ou foi movida.</p>
            <div class="error-actions">
                <a href="/dashboard" role="button" class="contrast">Voltar ao Início</a>
                <a href="javascript:history.back()" role="button" class="outline">Voltar à Página Anterior</a>
            </div>
        </div>
    </article>
</div>

<style>
    .error-page {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
    }

    .error-page article {
        max-width: 600px;
        width: 100%;
        animation: fadeIn 0.5s ease-in-out;
    }

    .error-page h1 {
        font-size: 6rem;
        margin: 0;
        color: var(--primary);
    }

    .error-subtitle {
        font-size: 1.5rem;
        margin: 0 0 2rem 0;
        color: var(--muted-color);
    }

    .error-content {
        margin: 2rem 0;
    }

    .error-content p {
        margin-bottom: 2rem;
    }

    .error-actions {
        display: flex;
        gap: 1rem;
        justify-content: center;
    }

    @media (max-width: 768px) {
        .error-page h1 {
            font-size: 4rem;
        }

        .error-actions {
            flex-direction: column;
        }

        .error-actions a {
            width: 100%;
        }
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(-20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>